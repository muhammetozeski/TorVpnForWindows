<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <RootNamespace>TorVpnForWindows.Tests</RootNamespace>
    <AssemblyName>ControlReplyTest</AssemblyName>
    <Platforms>x64</Platforms>
    <PlatformTarget>x64</PlatformTarget>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>

  <PropertyGroup>
    <AppDir>$(MSBuildProjectDirectory)\..\..\src\TorVpnForWindows</AppDir>
  </PropertyGroup>

  <ItemGroup>
    <!-- The control client and the entry lookup as they ship, compiled from the application sources. -->
    <Compile Include="$(AppDir)\Core\TorControlClient.cs" />
    <Compile Include="$(AppDir)\Core\EntryNodeChecker.cs" />
    <Compile Include="$(AppDir)\Core\Log.cs" />
    <Compile Include="$(AppDir)\Core\AppPaths.cs" />
    <Compile Include="Program.cs" />
  </ItemGroup>

</Project>
